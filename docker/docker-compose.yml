services:
  app:
    container_name: timeflow-website
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: production
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production